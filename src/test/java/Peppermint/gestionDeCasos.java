package Peppermint;

import insumosPeppermint.robotBasePeppermint;
import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.layout.GridPane;
import javafx.stage.Stage;

import javax.swing.*;

/*public class gestionDeCasos extends Application {

    public static void main(String[]args){
        Application.launch(gestionDeCasos.class);
    }
    public void start(Stage stage1){

        JTabbedPane pestañas=new JTabbedPane();
        GridPane panel1=new GridPane();
        botCREATOR botCreacion = new botCREATOR();
        panel1.add(botCreacion);

        pestañas.addTab("Creacion", panel1);

        Scene scene = new Scene (panel1,300, 500);
        stage1.setScene(scene);
        stage1.show();
    }

}*/
